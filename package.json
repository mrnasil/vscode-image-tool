{
  "name": "vscode-image-tool",
  "version": "1.0.0",
  "description": "VS Code extension to download images and capture website screenshots.",
  "engines": {
    "vscode": "^1.60.0"
  },
  "scripts": {
    "test": "echo \"No tests available\" && exit 0",
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "keywords": [
    "vscode",
    "extension",
    "image",
    "screenshot"
  ],
  "author": "mrnasil",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "ofetch": "^1.3.3"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.2",
    "@types/node": "^16.7.10",
    "@types/vscode": "^1.83.0",
    "typescript": "^4.4.4"
  }
}
